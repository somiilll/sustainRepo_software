<analysis>**original_problem_statement:**
The user is building a multi-tenant Greenhouse Gas (GHG) calculation platform with three roles: Super Admin, Admin, and User.

The primary goal is to create a sophisticated and flexible emission calculation engine managed by the Super Admin. This involves:
1.  A **Fuel Database** where the Super Admin can define fuel parameters (, , , etc.) categorized by  and .
2.  A **Formula Management Module** for the Super Admin to define calculation logic, parameters, units, and unit conversions.
3.  A strict, backend-driven **Unit Normalization System** to ensure all calculations are performed consistently, converting all inputs to a base unit (e.g., kg) before calculation.
4.  The final emission calculation for Users/Admins must use the data from the Fuel Database and the logic from the Formula Module.

**PRODUCT REQUIREMENTS (Latest/In-Progress):**
-   **Canonical Internal Formula:** All calculations must resolve to a -based energy input using the formula: .
-   **Unit Normalization Engine:** The backend must automatically convert user-provided quantities (e.g., from grams, tonnes, litres, m³) into a standard  before any calculation. This requires density for volume-to-mass conversions.
-   **Gas-wise Emission Output:** The system must calculate and store four distinct values: , , , and total . The  value is calculated by applying Global Warming Potential (GWP) factors to CH₄ and N₂O.
-   **Formula Module (Super Admin):** A UI for the Super Admin to define parameters, their standard units, possible conversion units, and whether a parameter is user-provided or predefined. It should also allow defining the calculation formulas themselves.
-   **Admin View:** Admins should temporarily be able to see the detailed calculation breakdown on the Add Emission form for verification purposes. End-users should only see the final four emission results.
-   **Fuel Uniqueness:** A fuel is considered a duplicate only if the combination of , , , and  already exists.

**User's preferred language**: English

**what currently exists?**
The application is a full-stack platform (React + FastAPI + MongoDB). A Fuel Database module exists for the Super Admin to manage fuel properties. An Emissions page allows users to record emissions, which now uses the Fuel Database for its calculations.

The agent has gone through several iterations of the calculation logic and the supporting modules. It initially built a simple formula, then a more complex one, then a UI for managing formulas. Most recently, the user provided a very strict, detailed specification for a unit normalization engine. The agent has started refactoring the backend to implement this new engine, which is currently in a half-finished state.

**Last working item**:
-   **Last item agent was working:** The agent was in the process of a major refactor of the backend emission calculation logic to implement a strict unit normalization system as per the user's latest detailed requirements. It had just updated the Pydantic models in  to support this new structure and was about to replace the core  function.
-   **Status:** IN PROGRESS
-   **Agent Testing Done:** N (for the new unit normalization logic)
-   **Which testing method agent to use?** both. After completing the backend refactor, the frontend  page will need to be updated to align with the new data structure. A full end-to-end test using the testing agent will be required.
-   **User Testing Done:** N

**All Pending/In progress Issue list**:
-   **Issue 1 (P0): Complete the Unit Normalization and Formula Engine Refactor.** The current calculation logic is being replaced. The work is incomplete, leaving the backend in a non-functional state for emission calculations.

**Issues Detail:**
-   **Issue 1: Complete the Unit Normalization and Formula Engine Refactor**
    -   **Attempted fixes:** The agent has started the refactor. It defined new Pydantic models (, , ) in  to represent the user's new requirements.
    -   **Next debug checklist:**
        1.  Resume work in .
        2.  Replace the existing  helper function with the new version that performs unit normalization for  and  before calculating emissions.
        3.  Ensure the  and  endpoints use this new function correctly.
        4.  Update the  model if necessary to return the four required emission values (, , , ).
        5.  Test the backend endpoints thoroughly with  to validate the new normalization and calculation logic.
        6.  Update the frontend  to correctly display the four output values and ensure the Admin-only calculation view works with the new structure.
    -   **Why fix this issue and what will be achieved with the fix?** This is the user's highest priority requirement and will create a robust, accurate, and extensible calculation engine, which is the core of the application.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** N
    -   **Should Test frontend/backend/both after fix?** both
    -   **Blocked on other issue:** None

**In progress Task List**:
-   **Task 1: Complete the Formula Management Module UI.**
    -   **Where to resume:** The UI at  has been created with tabs for Formulas and Parameters. It allows creating parameters and defining formulas. However, it needs to be aligned with the new backend unit normalization logic.
    -   **What will be achieved with this?** The Super Admin will have a fully functional UI to manage the entire calculation framework as per their requirements.
    -   **Status:** IN PROGRESS
    -   **Should Test frontend/backend/both after fix?** both

**Upcoming and Future Tasks**
-   **Upcoming Tasks:**
    -   **P1:** Implement the Forgot Password feature. The UI exists but the backend logic is missing.
-   **Future Tasks:**
    -   **P2:** Refactor the monolithic  into a structured package (e.g., routes, models, services).
    -   **P2:** Refactor large frontend components into smaller, reusable pieces.
    -   **P2:** Implement full SMTP integration for notifications and password resets.

**Completed work in this session**
-   **Fuel Database Integration:** Integrated the Fuel Database into the  page, replacing the old category system with a fuel selection dropdown.
-   **Fuel Database Fixes:**
    -   Updated the fuel duplicate check logic twice based on user feedback. It now correctly validates uniqueness based on .
    -   Fixed UI issues on the  page, including adding  as a unit, removing a conversion section, and improving error handling for empty states and creation failures.
-   **Iterative Calculation Logic:**
    -   Implemented a backend calculation that produced four outputs (, , , ).
    -   Updated the  UI to hide the calculation breakdown from regular users, showing only the final four results.
-   **Admin-Specific View:** Implemented a feature to show the detailed calculation breakdown on  only when the logged-in user has the Admin role.
-   **Formula Module Scaffolding:** Created a new Formula Management module for the Super Admin, including backend models/endpoints and a frontend page () with tabs for defining formulas and parameters.

**Earlier issues found/mentioned but not fixed**
-   **Issue 1: The Forgot Password feature is incomplete.**
    -   **Debug checklist:** Implement  and  endpoints in . Integrate an email service for sending reset links.
    -   **Why to solve this issue and what will be achieved with this?** This is a standard and essential feature for user account management.
    -   **Should Test frontend/backend/both after fix:** Both.
    -   **Is recurring issue?** N

**Known issue recurrence from previous fork**
-   None.

**Code Architecture**


**Key Technical Concepts**
-   **Backend:** FastAPI, Pydantic, MongoDB (), JWT Authentication.
-   **Frontend:** React, React Router, Axios, TailwindCSS, Shadcn UI,  for user role.

**key DB schema**
-   **fuels:** 
-   **formula_parameters:** (New) 
-   **formula_definitions:** (New) 
-   **emission_records:** (Updated) Now stores , , , .

**All files of reference**
-   
-   
-   
-    (New)
-   
-   

**Areas that need refactoring**:
-   The monolithic  is the top priority for refactoring once the current feature set is stable. It contains all models, routes, and business logic.
-   The  component is very large and contains complex state management that could be broken down into smaller hooks and components.

**key api endpoints**
-   **New:**
    -   
    -   
-   **Updated:**
    -   : Logic is being completely rewritten.
    -   : Logic is being completely rewritten.
    -   : Duplicate check logic was updated.

**Critical Info for New Agent**
-   You are in the middle of a major, user-directed refactor. The previous calculation logic is now obsolete.
-   Your primary task is to **complete the implementation of the unit normalization engine** in  as defined by the user in message #280. The backend is currently broken for emission calculations until this is finished.
-   **Do not** revert to any previous calculation methods. The new canonical formula is .
-   After fixing the backend, you must ensure the frontend  page and the  module are aligned with the new, rigorous backend structure.

**documents and test reports created in this job**
-    (Updated)
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User (LATEST):** Provided a highly detailed, structured specification for a new unit normalization engine and calculation formula. This is the new source of truth for the calculation logic.
2.  **User:** Pointed out that the Formulas module was missing the ability to define the actual formula equation.
3.  **User:** Requested that admins (but not users) be able to see the calculation breakdown.
4.  **User:** Provided multiple new requirements for the Formula module, including defining parameter units, conversions, and whether they are user-input or predefined.
5.  **User:** Confirmed that a fuel can exist multiple times if its  or  is different.
6.  **User:** Requested a stricter duplicate check for fuels: .
7.  **User:** Reported several bugs on the Fuel Database page: Operation failed error on creation, incorrect empty state handling, missing  unit, and an unwanted conversion section.
8.  **User:** Provided a corrected, clean version of the emission calculation logic, asking for 4 distinct outputs (, , , ) and hiding the calculation details from end users.
9.  **User:** Provided detailed product requirements for the emission calculation formula, including an example.
10. **User:** Started the session by asking to complete the P0 task of integrating the Fuel Database.

**Project Health Check:**
-   **Broken:** The core emission calculation functionality is currently broken due to an in-progress refactor of the backend logic. The application is not in a shippable state.

**3rd Party Integrations**
-   **python-docx:** For generating  reports.
-   **aiosmtplib:** Installed but not in use.
-   **python-multipart:** For file uploads in FastAPI.
-   **date-fns:** For date formatting on the frontend.
-   **react-day-picker:** For calendar UI on the frontend.

**Testing status**
-   **Testing agent used after significant changes:** YES, it was used to verify the initial Fuel DB integration.
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None.
-   **Known regressions:** The entire emission calculation feature is currently non-functional due to the refactor.

**Credentials to test flow:**
-   **Super Admin:**  / 
-   **Admin:**  / 
-   **User:**  / 

**What agent forgot to execute**
-   The implementation of the new, strict unit normalization engine is the main incomplete task.
-   The Forgot Password feature remains on the backlog.</analysis>
