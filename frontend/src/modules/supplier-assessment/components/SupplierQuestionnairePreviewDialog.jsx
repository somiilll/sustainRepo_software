import React from 'react';
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle } from '../../../components/ui/dialog';
import { Button } from '../../../components/ui/button';
import { Input } from '../../../components/ui/input';
import { Checkbox } from '../../../components/ui/checkbox';

export const SupplierQuestionnairePreviewDialog = ({ open, onOpenChange, questionnaire, questions }) => <Dialog open={open} onOpenChange={onOpenChange}>
  <DialogContent className="max-h-[calc(100dvh-2rem)] max-w-2xl overflow-y-auto bg-white" data-testid="supplier-questionnaire-preview-dialog"><DialogHeader><DialogTitle>Supplier preview — {questionnaire?.name || 'Questionnaire'}</DialogTitle></DialogHeader><div className="space-y-5">{questions.length === 0 ? <p className="text-sm text-stone-500" data-testid="supplier-questionnaire-preview-empty">Add questions to preview the supplier experience.</p> : questions.map((question, index) => <div key={question.id || index} className="rounded-lg border border-stone-200 bg-white p-4" data-testid={`supplier-question-preview-question-${question.id || index}`}><p className="text-sm font-semibold text-stone-900">{index + 1}. {question.question_text}{question.required && <span className="ml-1 text-rose-500">*</span>}</p><div className="mt-3">{question.response_type === 'yes_no' ? <div className="flex gap-2"><Button variant="outline" size="sm">Yes</Button><Button variant="outline" size="sm">No</Button></div> : question.response_type === 'dropdown' ? <select className="h-10 w-full rounded-md border border-stone-300 bg-white px-3 text-sm"><option>Select an option</option>{(question.options || []).map((option) => <option key={option.value} value={option.value}>{option.label || option.value}</option>)}</select> : question.response_type === 'text' ? <textarea className="min-h-20 w-full rounded-md border border-stone-300 p-3 text-sm" placeholder="Supplier response" /> : <Input type="number" placeholder={question.response_type === 'percentage' ? '0%' : 'Enter a value'} />}</div></div>)}</div><DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)} data-testid="close-supplier-questionnaire-preview">Close preview</Button></DialogFooter></DialogContent>
</Dialog>;