"""
Shared contracts — request/response schemas.

Future phases will populate this with Pydantic models grouped by domain
(api/, calculations/, uploads/, reports/, dashboard/) per the Phase B1
plan. The directory is created now so other modules can begin importing
from a stable path.
"""
