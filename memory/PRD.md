# ESG Platform — Product Requirements Document

## Original Problem Statement
Maintain the product roadmap and repository health while improving the Supplier Portal, ESG modules, Organization settings, and GHG workflows. Keep manual entry, Bulk Upload, reporting, supplier assessment, and administrative workflows aligned with canonical configuration.

## Product Goal
Provide dependable, organization-aware ESG and GHG management for customer organizations and their suppliers, with secure records, immutable calculation history, and consistent module access.

## User Personas
- **Super Admin:** Configures organizations, plan limits, module access, workflows, and organization-scoped accounts.
- **Organization Admin:** Manages organization details, facilities, emissions, ESG records, suppliers, documents, training, targets, and reports.
- **Assigned User:** Manages records permitted by organization, facility, KPI, and workflow assignment.
- **Supplier User:** Completes assigned ESG, GHG, document, training, and revenue requirements through immutable submissions.

## Core Requirements

### Canonical Configuration
- `organization_config` is the single source of truth for module entitlements and GHG capability overrides.
- All manual, bulk, supplier, report, and target routes use canonical configuration resolvers.
- Do not introduce route-local authorization or shadow configuration.
- `/app/backend/calc_engine/` is frozen unless separately approved.

### Organization Module
- Organization Details uses responsive **Basic Details**, **GHG Details**, **Production Data**, and **Revenue Data** tabs.
- GHG Details requires the canonical `environment.ghg` entitlement; Production and Revenue require an enabled Environment, Social, or Governance entitlement.
- Production Data contains Production Quantity only; Revenue Data contains Turnover / Revenue only, for the active year and four preceding years.
- Organization and Facility details remain responsive, concise, and use neutral supporting copy for equity-share guidance.

### GHG Data Entry
- Support monthly and yearly Scope 1, Scope 2, Scope 3, and approved biogenic entries.
- Preserve common Create/Edit contracts, calculation audit linkage, approval behavior, and atomic monthly multi-row submissions through `submission_batch_id` rollback.
- Enforce organization capability overrides server-side.
- Resolve density visibility and requiredness from actual quantity/reference-unit dimensions.
- Preserve source values while normalizing valid reverse unit conversions before calculations reach the frozen engine.
- Scope 3 Activity menus retain a viewport-safe adaptive width for readable selection.
- Scope 3 Category 3 requires a canonical Activity Type of **Fuel**, **Electricity**, or **Steam** before Activity selection; the type limits visible C3 factors without changing calculations or historical emission records.
- Scope 3 Categories 5 and 12 use one durable disposal taxonomy: users select a base Activity first, then only the disposal Activity Types valid for that material. If an Activity has no disposal variant, such as Waste Water Treatment, no Activity Type field is shown. Legacy combined names such as `Mixed Plastics - Recycled` are resolved presentation-only into Activity + Activity Type while factor IDs and original combined activity strings remain immutable for calculation and audit continuity.
- C4 and C9 calculation-field labels are configuration-driven; the UI must not override a mapped variable label based on category.
- Scope 3 `activity_basis` formulas and input-field configurations are isolated by these ten formula groups: **C1/C2**, **C3**, **C4/C9**, **C5/C12**, **C6**, **C7**, **C8**, **C10/C13/C14**, **C11**, and **C15**. A group owns its activity-branch formula family and mutable field mappings; no active formula or mapping is shared across groups. Spend-basis, supplier-basis, and factor records remain unchanged.
- Super Admins can inspect activity formula groups, category ownership, active mappings, formula counts, and decision-tree impact. Before publishing a formula change, Formula Builder shows every active category/branch affected; group-owned formulas direct admins to clone rather than reuse across groups.
- Formula Builder cloning supports two destination types: approved Scope 3 activity formula groups, and direct Scope 1 or Scope 2 categories/subcategories. Direct clones are independent records and never rebind a decision tree automatically.
- Scope 3 Bulk Upload and OCR GHG-save preserve the resolved activity formula’s group ID into Calc Engine execution. OCR activity-input hydration now prefers the matching group-owned mapping before legacy fallback; Bulk Upload receives matching group-owned default-unit and allowed-unit validation without changing workbook column names or unrelated method logic.
- C3/C5 Bulk Upload, OCR, and Manual Add/Edit persist the resolved factor’s canonical `scope3_activity_type`. Bulk preserves explicit C6/C7 spreadsheet values first, then falls back to the matched factor type for C3/C5; no new spreadsheet columns are required.

### GHG Period Row Allowance
- `entitlements.environment.ghg.monthly_rows_allowed` is an organization-wide allowance per distinct monthly period.
- Yearly allowance is `monthly_rows_allowed × 12` per distinct yearly period; blank means unlimited.
- Monthly and yearly quotas are independent; legacy monthly records count as monthly.
- Superseded supplier revisions do not consume the current-row allowance.
- Manual entry, C7, Bulk Upload preview/save, and bulk confirmation share the same enforcement rule.

### Supplier Assessment
- Preserve the module registry and immutable program-revision model.
- Supplier ESG and GHG submissions are immutable, with parent-controlled reopen/resubmission.
- Supplier GHG reporting derives from the active relationship and immutable program revision.
- Suppliers receive only assigned Scope 1 and/or Scope 2; Scope 3 and Biogenic are excluded end-to-end.
- Supplier final submissions require auditable verification acknowledgement.
- Soft-deleted or deactivated suppliers cannot retain application access.

### Security and Data Integrity
- Password-reset tokens must be hashed before persistence.
- Approved PII and financial fields require application-level AES-GCM encryption, with blind indexes where equality lookup is needed.
- Preserve plaintext operational GHG metrics required for aggregation/reporting.
- Evidence files require hash-based integrity verification.
- New emission records pin immutable formula and decision-tree versions; historical calculations use their stored version references.

### Bulk Upload
- Generate organization-aware Scope 1, Scope 2, and Scope 3 templates.
- C6 Business Travel bulk uploads do not request, validate, calculate with, or persist `No. of Days Travelled`; C7 retains its own travel-day field.
- Enforce scopes, categories, Process Emissions, Flaring, custom fuels, and plan limits through canonical resolvers.
- Provide dry-run totals, row-level errors/warnings, valid-row partial success, and rollback on persistence failure.
- Limit files to 10 MB, sheets to 5,000 rows, workbooks to 25,000 rows, and pending validated records to 24 hours.

## Architecture
- `/app/backend/modules/sustainability_config/` — canonical organization settings and GHG overrides.
- `/app/backend/modules/entitlements/` — module access and numeric plan-limit enforcement.
- `/app/backend/modules/emissions/` — manual GHG create/edit/history, C7, rollback, and contracts.
- `/app/backend/bulk_upload_scope3/` — Excel generation, parsing, validation, preview, and persistence.
- `/app/backend/modules/supplier_assessment/` — supplier programs, submissions, Documents, Training, and completion.
- `/app/backend/modules/ocr_invoice/` — native OCR extraction, classification, provider diagnostics, and OCR-to-GHG save flow.
- `/app/backend/calc_engine/` — frozen calculation decision engine.
- `/app/frontend/src/modules/ghg/` — shared GHG capabilities, form orchestration, adapters, and categories.
- `/app/frontend/src/pages/Emissions.js` — emission ledger and Edit orchestration.
- `/app/frontend/src/components/EmissionEntryForm.js` — Add Emission form orchestration.

## Primary Data Collections
- `organization_config` — entitlements, GHG overrides, organization settings, and aliases.
- `emission_records` — current GHG records and immutable supplier revisions.
- `emission_history` — emission version history.
- `ce_decision_trees` and `ce_decision_tree_versions` — immutable calculation logic versions.
- `ce_input_field_mappings` — configuration-driven field display, defaults, units, and ordering.
- `scope3_ef` — Scope 3 emission factors, including canonical C3 `activity_type` classification.
- `bulk_upload_jobs`, `bulk_upload_pending_records`, `bulk_upload_errors` — Bulk Upload workflow records.
- `supplier_relationships`, `supplier_assessment_programs`, `supplier_ghg_submissions` — supplier assignment/submission lifecycle.

## Key API Endpoints
- `POST /api/emissions`
- `POST /api/emissions/c7/month`
- `POST /api/emissions/c7/yearly`
- `POST /api/emissions/batch-rollback`
- `GET /api/scope3-ef`
- `POST /api/bulk-upload/scope3/upload`
- `POST /api/bulk-upload/scope3/jobs/{job_id}/save`
- `GET /api/organizations/my`, `PUT /api/organizations/my`

## Current Status — September 21, 2026
- **P1 planned:** retire the overlapping broad Scope 3 `subcategory_selection` mapping for C8/C10/C11/C13/C14 after group-owned resolver precedence and legacy-formula verification are in place.
- C3 Activity Type filtering and 32-factor canonical classification are complete. A rollback backup is stored at `/app/.emergent/backups/c3-activity-types-20260921T060449Z.json`.
- Staging catalog baseline migration is complete: all 31 staging formulas were already version-linked; 3 missing decision-tree snapshots and formula-version maps for all 23 current trees were written without modifying historical emissions. Staging C3 classification was also applied to 32 factors. Backups are stored at `/app/.emergent/backups/staging-version-baseline-20260921T063336Z` and `/app/.emergent/backups/c3-activity-types-20260921T063346Z.json`.
- The approved staging MongoDB-only GHG reset removed 7,070 emissions, history, pending approval, calculation audit, Scope 3 bulk-upload, supplier-GHG, and Base Year records/traces. The backup is `/app/.emergent/backups/staging-ghg-reset-20260921T065543Z`. Organizations, facilities, users, supplier accounts/relationships/programs, peer benchmarking, targets, sinks, ESG records, `uploaded_files`, and all R2 objects were preserved.
- `CORS_ORIGINS` explicitly includes the approved staging frontend origin and the hosted release origin. No wildcard origin is enabled.
- The temporary staging MongoDB URI, database-name configuration, and target-specific reset/bootstrap utilities were removed after the completed staging work. The retained generic catalog migration accepts target connection values only from runtime environment variables and contains no staging URI or database name.
- Removed the C4/C9 `km_travelled` frontend label override. Add and Edit now use `ce_input_field_mappings.field_label` directly, including a configured **Distance Travelled per day** label.
- Applied the local `test_database` Scope 3 activity-group migration: 20 new versioned group-owned activity formulas, 31 group-owned input-field mappings, and 15 decision-tree rebinds were added without deleting any legacy formula or mapping. C10 became the canonical activity branch for the required C10/C13/C14 group. A non-destructive repair then re-bound first-generation clones and deactivated 20 nested duplicate formulas plus 30 nested duplicate mappings created by a repeated migration invocation; all records are retained in backups.
- Added a Formula Builder impact panel and an in-place safe clone action that creates an independent formula without changing any decision tree. Formula Builder and Input Field Mapping both support category filtering; Input Field Mapping visibly identifies group-owned and inactive-history rows.
- Aligned OCR GHG-save and Scope 3 Bulk Upload calculation execution with activity formula groups. Both retain group identity while resolving the selected formula; OCR now selects the group-owned mapping for activity input defaults, while Bulk Upload uses that same group-specific mapping during Calc Engine unit validation.
- Expanded Formula Builder cloning so a source formula can be copied to any Scope 1 or Scope 2 category/subcategory, in addition to the approved Scope 3 group destinations.
- Added non-destructive C5 factor taxonomy fields to all 184 C5 records in `test_database`: `activity_name`, normalized `activity_type`, and `activity_type_label`. The shared Add/Edit selector now exposes a disposal type before the base Activity. Original C5 activity strings, factor IDs, units, calculations, and existing emission records were not changed. Backup: `/app/.emergent/backups/c5-activity-taxonomy-20260921T090000Z`.
- Revised the C5 Add/Edit interaction to show the deduplicated base Activity selector before Activity Type; other Scope 3 categories retain their existing Type-before-Activity behavior.
- Refined C5 further: Activity Type options now filter to the selected base Activity, and the field is omitted for `other` taxonomy records such as Waste Water Treatment.
- Corrected C5 type-option derivation to use the complete matching C5 catalog rather than the already type-filtered Activity list. Users can now switch among all valid disposal types for the selected material.
- Corrected the C5 selector state model: the visible base Activity is now independent from the saved exact `scope3_ef_id`. Choosing a disposal type resolves the factor ID without clearing the selected material, while choosing a material does not retain an incompatible factor ID.
- C5 Edit places base Activity and Activity Type in one responsive row. The payload persists the exact `scope3_ef_id` and `scope3_activity_type`; base `activity_name` remains presentation taxonomy resolved from the referenced factor, not a separate emission-record payload field.
- C5 Edit recognizes the canonical `waste_generated_in_operations` category code in addition to display labels. Waste Water Treatment is classified as `other`, so its Activity Type control is suppressed.
- C5 Version History suppresses Activity Type change rows as display-only noise; audit values remain stored and other categories retain their Activity Type history.
- The Emissions **Reset widths** action sits at the right edge of the Scope 1 / Scope 2 / Scope 3 / Biogenic tab row and resets the same resizable ledger columns without affecting filters or records.
- For Scope 3 C8, C10, C11, C13, and C14, the Subcategory value remains `energy` for decision trees and factor filtering while its user-facing label is **Grid Power**.
- The Super Admin Scope 3 EF table, factor detail view, and edit selector use the same **Grid Power** display label for stored `energy` subcategories.
- Restored native Think OCR configuration parity with the uploaded standalone processor: the OpenAI Think provider now reads `OPENAI_API_KEY`, matching the native `OpenAI` SDK contract. The prior `OPEN_API_KEY_OCR` variable prevented Think mode from initializing after the wrapper removal. Source reviewed only; no functional testing was run by user instruction.
- OCR Extraction’s empty upload state is now a borderless, page-centered workspace. The existing source-document text, Upload Files action, and Download template action remain unchanged, while selected-file controls continue to use their compact workflow layout.
- Once source files are selected in OCR Extraction, the empty-state upload copy is replaced by a single-column file list. Each row shows filename, size, selected/uploaded timestamp, and remove action; the footer provides Process *n* files and Upload More Files actions.
- The OCR Fast/Think selector is now a compact rounded dark-teal toggle, with a distinct active mode while retaining its existing mode values and behavior.
- OCR selected-file staging now uses a capped, scrollable file box. Filename, size, and any validation/provider error share one responsive row; invalid files cannot be processed until removed, and queued failure rows also retain their file-specific error.
- OCR upload guidance now lists invoices, utility bills, receipts, CSV ledgers, and Excel workbooks, with an enforced 20-file batch limit, 20MB per-file limit, and first-15-PDF-pages processing behavior. Think is now the non-persistent default mode in both UI and API; Fast/Think uses a white shell with dark-teal selection.
- The scrollable OCR selected-files container uses rounded-2xl corners for a softer visual boundary.
- OCR now canonicalizes common Calc Engine mass and volume aliases before persistence and calculation: tonne variants become `t`, and litre variants become `L`. This normalization covers extracted values, factor selection, edit auto-save, direct GHG save payloads, and dynamic calculation inputs so the ledger retains canonical units.
- Added British-spelling unit coverage: `kilolitres` now normalizes to `kL`, while `millilitre` and `millilitres` convert their quantity to litres (`L`) before OCR calculation persistence.
- OCR workspace cleanup now clears Batch Queue state when Clear Workspace succeeds. When the final source file’s rows are all saved to GHG or rejected—individually or in bulk—the workspace automatically returns to the empty upload state.
- The Emissions ledger now hides Reset widths until a column has an actual manual width override. Resetting removes the override and hides the control again.
- Scope 1, Scope 2, Scope 3, and Biogenic navigation now uses compact, rounded responsive pills. The selected scope uses dark emerald green aligned with Add Emission, while controls wrap safely on small screens.
- Completed a database-only Calc Engine input cleanup: removed 30 inactive duplicate field-mapping configurations after confirming each had an active same-category replacement and no references in formulas, decision trees, emission records, or history. No active field or stored emission value was removed.
- Removed the two unreferenced, completed one-off staging utilities (`backend/scripts/migrate_local_to_staging.py` and `scripts/ingest_amns_to_staging.py`). No executable code now contains staging Mongo target variables or the staging database name; runtime database access remains environment-driven.
- Expanded developer-facing Supplier Assessment runtime logging. Invitation, reminder, assignment, and unlock email paths now emit structured sent/failed/skipped/deduplicated events; GHG, questionnaire, document, revenue, and supplier-access lifecycle transitions emit explicit locked/unlocked events with safe IDs and no recipient-email PII.
- Completed remaining Supplier Assessment debug-log coverage: document publication, due-date changes, assignment synchronization, and archival; program-revision creation; scoring-engine start/completion/fallback plus canonical-score refresh; and training creation, assignment synchronization, update, and archival now emit structured runtime events. Existing route-level rejection/failure events complement these service-level traces.
- Supplier creation now collects and persists an optional Vendor Code before Company Name. The supplier portal’s Revenue Information module is now Org Information without the prior parent-name description; it adds Parts/Components Manufactured and Location of the plant fields with parent-company contextual tooltips, persisted in both relationship drafts and submitted snapshots.
- Completed the parent-facing Supplier Assessment unlock flow: supplier eye detail no longer shows the generic locked ESG submission panel, displays Parts/Components Manufactured and Plant Location, and provides a submitted Org Information unlock action. Questionnaire and Document Manage Suppliers dialogs now expose targeted unlock controls for submitted responses. Org Information reopen creates an editable revision while retaining the submitted audit record, resets completion state, and sends the existing supplier-unlock notification. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per the user’s instruction.
- Corrected the parent-facing unlock details: Org Information now exposes the actual submission date from the current submitted audit record, including legacy submitted records. Document unlock actions now carry the supplier-specific document requirement ID rather than the grouped document card’s representative ID, preventing multi-program/revision assignments from returning “Agreement not found.” **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per the user’s instruction.
- Completed the user-approved archival cleanup: deleted the standalone Facility E diagnostic, the unreferenced supplier-document migration module, and 24 named backfill/migration/repair/normalization/seed utilities. Retained all named regression tests and non-matching operational scripts. Added structured `log_event` lifecycle, rejection, storage-failure, provider-failure, processing-failure, completion, and facility-assignment events across advanced OCR; Bulk Upload now includes organization context plus structured invalid-workbook and save-rejection events. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per the user’s instruction.
- Reset the user-confirmed `sustainrepo_staging` database to a clean platform baseline. Created and checksum-verified full backup at `/app/.emergent/backups/staging-reset-20260922T094706Z/`; deleted 6,491 user/company activity documents from 94 collections without dropping any collection/index; preserved 30 platform reference catalogs including 3,157 Scope 3 EFs and 502 fuel records; created the single platform superadmin documented in `test_credentials.md`. Database verification confirms no non-reference documents remain, all retained catalog counts are present, and idempotency TTL/unique indexes remain. **DATABASE VERIFICATION ONLY; NO API OR BROWSER TESTING**.
- Fixed the deployment `ResolutionImpossible` conflict by removing the entire unused Google/Gemini gRPC dependency chain after confirming no runtime imports. This includes `google-generativeai`, `google-genai`, Google API/auth packages, `grpcio`, `grpcio-status`, `protobuf`, and their unused transitive packages. Regenerated `backend/requirements.txt` via pip, ran `pip check` successfully, restarted the backend, and replaced a malformed `.gitignore`. Deployment readiness now passes. Remaining non-blocking security follow-up: add `memory/test_credentials.md` to ignore policy and migrate credentials out of the manual `seed_esg_superadmin.py` script. 
- Synced the 30 user-confirmed platform reference collections from local MongoDB to `sustainrepo_staging` with no pre-overwrite backup by explicit request. This replaced the staging catalog documents for factors, fuels, currencies, formula configuration, calculation-engine mappings/units/trees/versions, and ESG definitions. BSON fingerprints match local for every collection; `ce_formula_versions` (145) and `ce_decision_tree_versions` (140) were copied exactly from local with no version-number rewrite. **DATABASE VERIFICATION ONLY; NO API OR BROWSER TESTING**.
- Implemented organization-aware Spend-Based currency fallbacks. Monthly Scope 3 calculations now prefer an exact monthly rate, then use the active organization’s current FY annual record (using its configured fiscal start month) or CY annual record. The shared resolver applies this to standard currency conversion, PPP, and inflation; manual calculation/default display, Scope 3 Bulk Upload, and OCR reviewed-row saving now all pass organization reporting settings. Audit source labels mark annual values as fallbacks. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING**.
- Added one-level ESG questionnaire subquestions. Parent questions can create multiple always-visible follow-up questions; each follow-up has its own response type, required/evidence settings, scoring configuration, and weight while inheriting the parent’s ESG category. The backend preserves parent-child ordering, duplicates relationships safely, and cascades parent deletion to active follow-ups. Supplier answer, preview, overview, and parent response-review views now group follow-ups below their parent; scoring continues to process every question independently. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING**.
- Fixed questionnaire loading regression from the subquestion hierarchy change: `get_questionnaire` now calls the module-level ordering helper directly instead of a missing `SupplierAssessmentService` delegate, resolving the 500/“Failed to load questions” failure without changing questionnaire data. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING**.
- Reordered existing liquid-volume allowed units for 20 C3 Fuel `scope3_ef` factors to `L`, `kl`, `ml`, `m3`, `cm3`, without adding/removing allowed units or changing `default_unit`. Eight mass-only C3 Fuel factors remain unchanged. Pre-update backup: `/app/.emergent/backups/c3-fuel-allowed-units-order-20260921T121758Z.json`.
- Reordered allowed units for 25 C8 `scope3_ef` factors that already include `L` to `L`, `kl`, `ml`, `m3`, `cm3`. No factor received a new `L` unit, 16 C8 factors without `L` remained untouched, and `default_unit` values were preserved. Pre-update backup: `/app/.emergent/backups/c8-existing-l-allowed-units-order-20260921T122837Z.json`.
- Reordered allowed units for 100 C10/C11/C13/C14 `scope3_ef` factors that already include `L` to `L`, `kl`, `ml`, `m3`, `cm3`. No factor received a new `L` unit, 64 factors without `L` remained untouched, and `default_unit` values were preserved. Pre-update backup: `/app/.emergent/backups/c10-c11-c13-c14-existing-l-allowed-units-order-20260921T123020Z.json`.
- Extended liquid-volume unit availability for 48 Scope 1 `fuel_database` records: any existing `L`/`kl`/`ml`/`m3`/`cm3` fuel now allows all five in the ordered volume segment `L`, `kl`, `ml`, `m3`, `cm3`, while preserving mass-unit order and every `default_unit`. The 406 Scope 1 fuels without a liquid-volume unit remain unchanged. Pre-update backup: `/app/.emergent/backups/scope1-fuel-liquid-volume-units-20260921T123302Z.json`.
- OCR Extraction now includes a header-level History dialog with one row per uploaded source file, showing filename, uploader name, upload timestamp, and safe processing/resolution status. Clearing a workspace removes its source files and review rows but retains this audit metadata; History does not reopen source documents.
- OCR History now stores `full_name` (email fallback) for new uploads and resolves legacy `Unknown` entries through the stored uploader ID. File outcomes are labeled Saved, Partially Saved, Rejected All, Event Cancelled, or Error; active/unresolved entries retain truthful Processing or Pending Review states.
- OCR History is now widened and shows a row-level audit table under each extracted, non-cancelled source file: item extracted, scope, category, subcategory, reporting period, and row outcome. Newly resolved rows are retained as safe audit snapshots, allowing Partially Saved files to show which rows were saved or rejected.
- OCR History now presents both Rejected and Rejected All outcomes with an orange audit-status treatment for immediate visual distinction.
- Detailed implementation history is in `/app/memory/CHANGELOG.md`.
- Prioritized remaining work is in `/app/memory/ROADMAP.md`.
- September 22, 2026 — Questionnaire Builder terminology and authoring controls now distinguish ESG **Section** (Environment, Social, Governance) from the persisted per-question **Category** (Policy, Reporting, Certification). The question ledger and single-question editor both write the new category field; existing questions safely default to Policy. Ledger parent rows now expose a visible **Follow-up** action that inserts an indented draft subquestion directly beneath its parent; draft parents save first and their follow-ups are then linked through the returned parent IDs. A follow-up inherits its parent Section, retains an independent Category, and stays visually synchronized if the parent Section changes. The question list restores compact Edit/Delete actions only, and duplication preserves both taxonomy fields. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — The Questionnaire Builder no longer uses a left-side questionnaire list. Questionnaire selection is now a dropdown beside Reporting Period, and a selected questionnaire uses the full workspace width. Its questions are organized in default-expanded, independently collapsible hierarchy: Environment, Social, Governance → Policy, Reporting, Certifications → corresponding question rows or clear empty states. Selected-questionnaire Edit, Duplicate, Activate/Deactivate, and Delete actions remain available as compact header controls. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Restored Questionnaire search in the control bar; it filters the adjacent Questionnaire picker without removing direct selection. Added distinct reusable heading treatments: Environment (emerald), Social (sky), Governance (amber), Policy (indigo), Reporting (rose), and Certifications (teal), including matching count badges. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Reordered the Questionnaire Builder control bar as requested: Search → Add Questionnaire → Select Questionnaire → Reporting Period. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — The Add Questions flow now opens a split workspace: a left navigation pane with Environment, Social, and Governance, each containing Policy, Reporting, and Certification; and a right question area for the active group. The right area displays matching configured questions below one direct-entry row. Saving keeps the workspace open, refreshes the matching list, and resets the row for the active Section/Category. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Restyled selected-questionnaire grouping to match the provided reference: clean white Section and Category accordions, icon-led labels, neutral round question-count pills, and nested grouped question tables/empty states. Icon accents retain visual distinction without competing colored panels. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Refined grouped question-table presentation to match the reference details: larger “Policy/Reporting/Certification Questions” titles, shaded Category accordion bars, a shaded Section/Category/Type/Field Type/Importance/Actions column-header band, numeric draggable row markers, and neutral Section/Category badges. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Moved Section and Category question-count pills into the right-side control cluster immediately before the accordion chevron, matching the reference alignment. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Restored full Add Questions authoring controls in the split workspace: black Environment/Social/Governance navigation labels; a Parent question / subquestion selector linked to current top-level questions; response type, scoring type, importance, evidence, and required controls; Yes/No score inputs; and dropdown options with per-option scores. Saving a selected-parent draft now persists it as a subquestion with sibling ordering. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Reworked Add Questions authoring as requested: reduced the left navigator width, removed the Parent Question dropdown, and restored an inline **Subquestion** action on the Question row. Question, Response Type, Scoring Type, Importance, Required, Subquestion, and Add Question now share one responsive row; scoring controls remain directly below. Adding a subquestion opens a complete indented child row and saves it after its parent in the same operation. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Corrected the Add Questions authoring-row breakpoint so Question, Response Type, Scoring Type, Importance, Required, Subquestion, and Add Question render together in one row at the standard desktop breakpoint, while retaining safe wrapping below that width. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Simplified new subquestion authoring: the indented child row now requests only Subquestion text and Remove. On save it inherits the parent Question’s Section, Category, response type, scoring type/configuration, importance, required setting, evidence requirement, and all score values. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Removed subquestion authoring/presentation from the Questionnaire Builder. Add Questions now uses neutral white draft rows: **Add Question** appends a new independent row in the active Section/Category, and **Save N Questions** persists all drafts together. Existing question presentation is flattened accordingly. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Save N Questions now uses the standard successful-save close path, closing the Add Questions popup after persistence. Removed configured-question index numbers from the popup list. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Restored desktop ledger presentation in Add Questions: a shaded, aligned column-header row for Question, Response Type, Scoring Type, Importance, Required, Evidence, and Actions; row-level labels remain on smaller screens. Restored Evidence selection (No evidence, Optional, Required) for every draft question. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Locked Add Questions header and draft rows to the same seven fixed grid tracks, eliminating header/field drift. Evidence now has only Not Required and Required. Replaced the Required checkbox with a **Question Mandatory** Yes/No dropdown. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Supplier evidence is now optional even when a questionnaire question is configured with Evidence = Required: removed both client and server submission blockers. For Yes/No questions, the supplier upload control is shown only after a Yes answer and hidden after No; existing attached files remain viewable. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Organisation-disabled modules are no longer removed from navigation. Disabled items remain visible in a muted, locked state; selecting them resolves to a shared unavailable screen instead of redirecting to Dashboard. A route-to-navigation access guard applies this pattern across static organisation modules, including Environment modules such as Energy. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Replaced the separate organisation unavailable screen with the exact shared Supplier locked-overlay treatment: blurred preview background, full-height white gradient overlay, emerald lock medallion, matching title/message hierarchy, and Contact Sales action. Supplier and organisation locks now use this single component. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Removed organisation-disabled lock glyphs from sidebar entries. Disabled modules remain visibly muted and clickable, matching the supplier navigation treatment without extra lock controls. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Split RepoPilot capability access. With Data Retrieval enabled and Internal Data AI disabled, RepoPilot Document AI remains available (documents, upload, filters, document chat), while Internal Data AI remains visible but muted and opens the shared supplier-style lock overlay. Backend guards now require `repo_pilot.data_retrieval` for RepoPilot document endpoints and `repo_pilot.internal_data_ai` for Internal AI chat. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Executive Analytics Dashboard no longer opens the bottom-right chart drilldown/title/Close panel on chart interaction. The Employees KPI now renders Female Workforce inline beside the employee value, eliminating the separate secondary KPI row. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Refined the Employees KPI secondary block: renamed it to **Female**, positioned it as a right-aligned stacked label/value, and applied percentage status colors (red below 30%, amber from 30% to below 50%, green at 50%+). The main employee count remains prominent on the left without changing the card footprint. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Matched the Employees KPI reference treatment by adding a Female profile-style icon beside the right-aligned Female label and moving dynamic status color into a compact dot beside the neutral percentage value. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Positioned the Female status block absolutely in the Employees KPI’s upper-right area so it no longer contributes to card height; it remains right aligned and the main employee value stays independent on the left. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Moved the Organization **Edit Details** control into the shared page header for admins, making it available from every Organization tab. Starting an edit now safely switches to Basic Details, where the existing edit form lives. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Restructured the Add/Edit Facility dialog into a wider clean split layout: title/subtitle header, responsive facility-details column, and a sticky Production Quantity column. Existing production data opens directly in edit mode; new facilities retain a concise save-first production state, while all existing facility fields and save behavior remain intact. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Corrected the Facility dialog surface and initial hierarchy to align with the provided reference: explicit white dialog/form/input surface, compact organisation-address control, core fields and Products/Machinery/Process shown first, and secondary facility fields collapsed under Additional facility details. Removed hidden Person Responsible mandatory validation so the simplified first view can save without expanding advanced fields. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Refined the Facility form: moved Use organisation address into the top header; aligned Facility Name/Sector/Sub-Sector and location fields on consistent grids; replaced sector/equity helper copy with hover Info hints; removed the Equity Share left border and default-value message; and added a create-time Production Quantity draft that persists after the new facility is created. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Refined Facility Production Quantity: Financial Year and Input Type now share a row; mode buttons are pill shaped; monthly inputs use four columns per row; and negative quantities are prevented in the create form, production editor, persistence payloads, and backend validation. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 22, 2026 — Follow-up Facility form corrections: Unit is now free text in create/edit Production Quantity; monthly input grids use three columns; add-time Annual/Monthly controls are visible and persist the appropriate yearly/monthly payload; core Facility/Sector/Sub-Sector rows use aligned tracks; Equity Share has a deliberate top gap; and Sector/Equity help now uses rendered tooltips. No persisted Facility data fields were removed—secondary fields remain under Additional facility details. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- **Testing constraint:** The user requires source review only; do not run functional, screenshot, curl, or testing-agent checks unless that instruction changes.
- September 22, 2026 — Implemented the R2 draft-only emission-evidence workflow. New files are held in local form state and reach Cloudflare R2 only after the related emission record save succeeds; draft removals never call storage. Post-save attachment now updates the saved generic or C7 employee-period record, while staged deletions run only after the primary update succeeds. The attachment path cleans up an R2 file if its record-link operation fails. Organization R2 key construction now trims whitespace before sanitization and removes trailing separator underscores, preventing prefixes such as `Org1_/`. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Completed the R2 draft-only workflow across Organization Details, Facilities, ESG Records (both legacy and operational entry components), and Sinks. File selection now creates local browser previews only; new objects upload after the primary record save succeeds, successful uploads are linked in a follow-up persistence step, and newly uploaded objects are cleaned up when record linking fails. Existing-file removals are queued until save; Sink storage cleanup now runs after its MongoDB update. Auto-save preserves persisted Organization/Facility attachments so it cannot commit staged files. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL, API, BROWSER, SCREENSHOT, OR TESTING-AGENT EXECUTION** per user instruction.
- September 23, 2026 — Corrected committed GHG emission-evidence storage-key generation. The internal attachment route now explicitly passes `folder=None` into the shared upload handler, preventing FastAPI `Query(...)` metadata from being stringified as an R2 folder. With an organization name available, GHG evidence uses `{org-name}/{YYYYMMDD}/{unique-file}`. Organization Details, Facilities, Sinks, and both ESG Records flows were source-reviewed: they use normal HTTP uploads, so FastAPI resolves their folder values correctly; ESG Records intentionally retains its section folder. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Reworded customer-facing evidence selection confirmations across GHG entry/edit, Sinks, Organization Details, Facilities, and ESG Records. Messages now use **File uploaded**, **Evidence uploaded for April**, or equivalent concise uploaded language; no customer-facing evidence confirmation says “staged.” The draft-only storage timing is unchanged. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Added Activity Type selection to Scope 3 C4 (Upstream Transportation) and C9 (Downstream Transportation): **Van, Sea Tanker, Cargo Ship, Road - HDV, Air, Waterways, and Rail**. Create and Edit share one transport classifier that filters existing factor activities by their canonical label prefixes, without changing factors, units, formulas, or calculation methods. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Replaced temporary C4/C9 UI name inference with the database-backed factor-classification model used by C3/C5. Applied the idempotent `classify_c4_c9_transport_activity_types.py` migration without a backup: 152 of 180 C4/C9 Scope 3 EF records now store `activity_type` as `van`, `sea_tanker`, `cargo_ship`, `road_hdv`, `air`, `waterways`, or `rail`; the 28 unmatched factors remain blank for review. Create/Edit filters now read `scope3_ef.activity_type` directly. **MIGRATION EXECUTED; SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Fixed draft-only GHG Edit selection transitions. Scope, biogenic scope, category, and Scope 3 method changes now reset all incompatible local fields and calculation previews together; stale form-config responses are ignored, and saved formula/decision-tree versions are used only while the original selection remains unchanged. No update payload is sent or persisted until the user selects Save. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Refined dashboard emissions visuals: Facility-wise **All** bars now use a neutral Total color distinct from Scope 1; GHG and Executive scope donuts have contiguous slices; Executive **GHG Emission Trend** now renders current Scope 1–3 plus a derived **Total Emissions** trend instead of Previous Year. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Updated GHG dashboard navigation copy from **View all categories** to **View all GHG data**. Executive chart tooltips now retain each series label, so GHG Trend hover content shows named values such as **Total Emissions**, **Scope 1**, **Scope 2**, and **Scope 3**. Confirmed Executive donut slices use zero angular padding. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Fixed GHG Edit’s stale decision-tree error when changing Scope/Category/Method. Form-config and live preview calculations now always resolve against the active category configuration; prior version bindings are never sent during edit previews, stale calculation responses are ignored, and version rebinding occurs only when the changed record is saved. The persisted record remains unchanged until Save. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Hardened the GHG Edit version-transition fix for already-open dialogs. Edit-session and selection changes now clear any prior calculation error/result; calculator responses are revision-guarded, and the backend preview endpoint falls back to the active category tree if a stale browser request contains a historical version ID. Non-preview saves remain version-safe and only rebind after the user selects Save. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Restored the GHG page after an Edit-calculator runtime crash. The stale-response guard introduced `useCallback` in `useEmissionsCalculator`; the missing React import was added. The frontend rebuild completes with pre-existing hook warnings only. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Fixed C5 Edit’s initial unit mismatch. On C5 Scope 3 EF selection, the locally stored unit now initializes from the same first allowed unit shown in the selector; preview calculation uses the same priority, preventing an initially displayed **kg** from being submitted as the EF default **t**. C7 category-transition loading remains identified for future work: a non-C7 record converted to C7 resets employees to an empty array while the C7 renderer treats an empty array as an endless loading condition. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Restored the GHG page after the C5 initialization update briefly referenced `filteredScope3Activities` before that memo was initialized. The initializer now resolves the selected EF from the earlier-loaded `scope3EFData` collection; the frontend rebuild completes with non-blocking pre-existing warnings only. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Fixed C7 category-transition loading in both active and legacy edit dialog paths. An empty C7 employee list now renders the existing **Add Employee** state instead of being treated as perpetual loading; only genuine record hydration shows a spinner. The C5 draft-unit initializer remains intentionally limited to C5; the shared Scope 3 EF calculation fallback was also aligned to the visible unit order as a defensive consistency measure. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Generalized draft unit initialization across all main GHG dynamic forms (Scopes 1–3 and biogenic). Fuel-backed, Scope 3 EF-backed, centrally configured, and static unit fields now persist the same first unit shown by their selector before calculation or Save; an explicit/saved unit is never overwritten. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Corrected Edit transitions into C7 and between Scope 3 categories. Selecting C7 now stores the record’s reporting month (including full labels such as **August 2026**) in the local C7 draft; already-open C7 dialogs self-initialize the same value, so monthly C7 shows one active month and yearly records show one yearly entry. When the calculation selection changes, original dynamic values/units are no longer rehydrated or displayed in the new category, preventing C3 `m3` inputs from leaking into C5. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Removed the false record-level validation path from C7 Edit. Employee Commuting now bypasses the generic `dynamicFieldValues` live preview and generic Save-time quantity check; its existing employee-period calculator and C7 submission validator remain the sole validation path. This prevents converted records from showing or being blocked by the incorrect **Distance Travelled per Day is required** warning. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL TESTING** per user instruction.
- September 23, 2026 — Corrected persistence and rehydration for records converted into C7. C7 Edit now writes canonical `employee_commuting` category identity, category ID, employee-period data, model-version metadata, reporting year/month metadata, and monthly totals; prior-category dynamic inputs and incompatible flat/gas fields are cleared. Previously converted records whose C7 label was saved with a stale category code now reopen through a C7-specific identity fallback and use the multi-employee form; saving them again canonicalizes their stored metadata. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Added the C5 Activity-first disposal taxonomy to C12 Add and Edit. C12 now groups legacy combined factor names by base Activity, shows only disposal Activity Types available for that Activity, infers saved legacy types from factor IDs on Edit, and hides Activity Type for variantless/`other` factors. C5 and C12 share one resolver, while original factor IDs, formulas, and calculation payload selection remain unchanged. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Hardened C12 Activity-first selection for new records. The shared disposal taxonomy now recognizes C12’s canonical code and display-name variants, and Add/Edit resolve matching C12 factors by canonical category identity rather than one exact label. New C12 entries therefore present a base Activity such as **Mixed Plastics** before its valid Activity Type options. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Persisted the C12 disposal taxonomy in `scope3_ef` by running the guarded `classify_c12_activity_types.py` migration. All 184 recognized C12 factors now store `activity_name`, canonical `activity_type`, and `activity_type_label`; original factor IDs, activity strings, units, calculations, and formulas were not changed. Backup: `/app/.emergent/backups/c12-activity-taxonomy-20260923T113634Z`. **MIGRATION EXECUTED; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Prevented stale Scope 3 selection metadata from surviving future direct conversions into Scope 1, Scope 2, or Biogenic Scope 1. The update route now unsets only Scope 3-only selection fields and removes their dynamic equivalents, while preserving valid Scope 3 and Biogenic Scope 3 data. New audit diffs use the same scope context, so non-Scope-3 transitions do not revive a prior Scope 3 Activity, Activity Type, or Calculation Method. Existing `emission_records` and existing `emission_history` entries were intentionally not modified. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Fixed C5/C12 untyped disposal activities such as Waste Water Treatment. The shared Add/Edit selectors now retain the factor’s canonical `other` activity type instead of clearing it, allowing the `other` form configuration and Quantity Used input to hydrate consistently on reopen. The shared display resolver now recognizes Scope 3 `activity_value` as a quantity. No existing records or audit entries were migrated or rewritten. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Refined OCR Extraction controls and ledger ergonomics. History, Upload Files, and Download template controls now use a light-teal surface distinct from the page. The desktop OCR review ledger now has GHG-style horizontal scrolling, draggable per-column widths persisted in browser storage, a keyboard-accessible bottom scrollbar, and a Reset widths action; the mobile review list is unchanged. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Refined GHG and reporting copy/layout. Yearly Add Emission has tighter heading-to-input spacing; Organization Details uses **Purpose of the GHG Report** in edit/view; GHG Inventory Report no longer shows an Available badge; calculated-emissions help says values are rounded to 2 decimal places. The GHG ledger’s Status column now sorts, and Reset widths is aligned to the far right of the scope row. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Removed the Reports-page **AI-Powered** badge and eliminated the fixed 48px label reserve in yearly Add Emission inputs. Quantity Used now follows its label with the normal 4px gap rather than an empty vertical block. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Aligned OCR Upload Files, Download template, and History with Bulk Upload’s primary action color. Removed the duplicate Refresh/Start another extraction control, retaining only Clear workspace. Removed the duplicate bulk-bar Select all control; the ledger header selector remains the single desktop select-all action. Also removed remaining fixed 48px label reserves from optional and override yearly-input paths. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Repaired the OCR review ledger JSX after the control cleanup. The malformed bulk-action container caused the React application error boundary immediately after OCR processing, even though uploads and OCR extraction completed successfully. No OCR uploads, extracted rows, or source files were changed. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Enforced a compact, explicit `space-y-1` label-to-input stack for required, optional, and override yearly fields, with fixed label heights overridden to zero and input rows lifted 12px to remove the remaining Quantity Used visual gap. Annual data and calculations were not changed. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Refined OCR review operations: the ledger’s horizontal scrollbar is permanently visible; bulk Save/Reject actions now share the filter row; Clear workspace uses the primary green action treatment. OCR edit dialogs now hide generated `row-###` placeholder invoice numbers while retaining genuine invoice numbers and vendor details. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Refined OCR Scope 3 display and defaults: empty/zero cost now renders as `—` rather than `USD 0`; missing edit currency defaults and remains INR through factor refreshes; reporting-period cells strip time components and show only the date. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Aligned the OCR ledger scrollbar with GHG behavior: the bottom track renders only when ledger columns overflow and supports the same horizontal wheel, track-click, keyboard, and draggable-thumb interactions. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Simplified the OCR review toolbar: removed the All rows dropdown and placed Save Selected, Reject Selected, Save All, Reject All, Search rows, and Scope in a fixed action-first toolbar order. Selected-only actions stay visible but disabled until rows are selected. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Corrected OCR ledger overflow mechanics by adding an explicit column-width content rail and deriving the scrollbar range from that rail rather than unreliable nested-table measurements. The GHG-style draggable track now appears whenever columns exceed the viewport. Selected-only OCR actions again stay hidden until at least one row is selected. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Corrected OCR edit defaults and source placeholders: reconciliation now defaults missing currency to INR; generated Excel `row-###` values are converted to blank, editable Invoice number inputs; generated Excel rows carrying the former USD fallback open with INR. The native ledger scrollbar is hidden and the custom draggable track uses neutral GHG stone colors. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Corrected Excel invoice payload handling. New spreadsheet ingestion no longer creates `ROW-###` placeholder invoice values. For legacy spreadsheet rows, the direct GHG-save route strips only generated `row-###` identifiers before calculation, persists a blank OCR invoice field, and prevents a fallback to the original placeholder when setting the final emission `record_source`. Genuine document invoice numbers remain unchanged. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Fixed direct OCR Save to match Edit dialog preparation for uniquely resolved factors. When a raw OCR unit/currency does not match an allowed alias, direct Save now uses that factor’s first configured allowed input—the same fallback already used by the Edit dialog—then persists the canonical factor/input before calculation. This removes the unnecessary Edit open/save round-trip for valid, unambiguous OCR rows. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Replaced the unsafe OCR factor-input fallback with explicit source-unit validation. Direct OCR Save now uses the actual `activity_value` unit for C3 and returns a clear error naming the extracted unit and selected factor’s allowed units; it does not substitute volume for mass. Edit auto-matching follows the same rule, stops before auto-persisting an incompatible factor/input pair, displays the allowed-units error, and leaves the source value untouched. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Corrected OCR factor selection behavior for incompatible extracted inputs. Edit now retains the automatically matched factor/subcategory (for example, Diesel), clears only the incompatible Unit/Currency selection, shows the factor’s allowed values, and keeps Save disabled until the user explicitly selects a supported input. This applies across Scope 1, Scope 2, and Scope 3 without changing extracted source values. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Corrected OCR mismatch highlighting. A direct-save response stating an extracted Unit/Currency is incompatible now removes the Subcategory requirement highlight and applies it only to Unit of quantity or Currency, while retaining the valid matched factor selection. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Isolated OCR input-mismatch validation from general required-field detection. When direct Save reports an incompatible extracted Unit/Currency, the Edit dialog now highlights only Unit of quantity or Currency; Facility, Reporting period, Subcategory, Quantity, and Cost are not added to that mismatch highlight state. Standard missing-field errors continue to highlight their actual missing fields. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Added a downloadable, formula-driven **GHG Emissions Summary (.xlsx)** option to Reports. The two-sheet workbook provides editable reporting information; Scope 1, Scope 2, all C1–C15 Scope 3, and biogenic sections; formula-linked Total GHG, Sinks, and Net Emissions; plus multi-facility allocation with formula totals. Unreported input values display as `-`; yearly records respect the selected period and are prorated consistently with the existing GHG report. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 23, 2026 — Reclassified the remaining OCR unit/currency mismatch isolation defect and immediate field-level validation-highlight clearing as **P0**, pending a focused source review and correction.
- September 24, 2026 — Corrected the GHG Excel export’s `NUMBER_FORMAT` literal. The previous malformed Python expression attempted to subtract string values during reports-module import, which could prevent backend startup/reload and consequently interrupt login. The format now uses a valid Excel zero-value display literal (`"-"`). **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Made the GHG Excel export discoverable in the Reports UI. The GHG Inventory card now includes a visible **Excel Summary** action that opens the report dialog with `.xlsx` preselected; the in-dialog Word/Excel output selector remains available. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Corrected Scope 3 values in the GHG Excel Summary. The legacy Scope 1–2 document filter was excluding Scope 3 records before workbook generation, resulting in C1–C15 rows displaying zero despite dashboard data. Excel now always receives all scopes, and both Excel entry points select Scope 1–3. C1–C15 aggregation continues to use the emission-record totals used by dashboard reporting. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Standardized the primary GHG reporting pipeline without changing the GHG Emissions Ledger, Base-Year Ledger, or core emissions engine. Added `shared/services/ghg_reporting.py` as the reporting-value authority: Decimal-based raw values, canonical reporting-period proration, equity-share allocation, unrounded aggregation inputs, and final `ROUND_HALF_UP` display rounding. Dashboard KPIs/facility summaries, GHG Word report formatting/proration/equity context, and Excel Scope/Facility/Sink totals now consume this shared policy. Excel now applies both period proration and facility equity share before aggregation, while retaining full precision until workbook insertion and displaying 2 decimals. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Repaired the Dashboard GHG summary outage introduced at the shared reporting boundary. Backend logs identified a `Decimal × float` error during `/api/dashboard/stats`, which caused a 500 response and left dashboard cards without values. The shared adjustment service now normalizes equity factors to `Decimal` for both emissions and sinks before multiplication. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Corrected the Dashboard/Excel reconciliation defect. Dashboard category aggregation now keys values by **scope + category**, preventing a Biogenic record (for example, Biogenic Stationary Combustion) from being merged into a Scope 1 category. Dashboard Total GHG Emissions and facility/trend totals now exclude Biogenic, matching the Excel definition; Biogenic remains visibly separate. The executive category card and legacy GHG summary cards display two decimals. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Simplified the GHG Excel reporting-information area: removed the redundant **Unit: tCO2e** and derived **Reporting Year** fields. Organization Name, Number of Facilities, Reporting Period, and Reporting Boundary remain editable. Emissions table headers continue to identify values as `tCO2e`. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Restructured the **Reports** action area (frontend only). GHG Inventory Report, GHG Excel, and AI Executive Summary are now equal responsive cards in that order: one column on mobile and three equal columns from tablet upward. The existing dialogs, APIs, calculations, permissions, loading/error behavior, and Report Contents section were not changed. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Repaired the Reports page JSX build error introduced during the three-card layout update. The report-actions grid wrapper was missing its closing tag; it now closes after the AI card and before Report Contents. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Refined the Reports action cards (frontend only): removed the Report Contents panel, reduced card height and padding, reduced icons, and positioned each title to the right of its icon. Existing report-generation behavior remains unchanged. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Simplified the GHG report dialogs (frontend only). Removed the historical-comparison checkbox and Output Format selector; GHG Inventory Report opens with `.docx` selected and GHG Excel opens with `.xlsx` selected through their dedicated cards. Reduced the description-to-button spacing on all three action cards. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Refined GHG report actions (frontend only): the Excel flow bypasses the all-facilities confirmation while retaining the actual all-facilities status, the historical-comparison checkbox is restored for the Word Inventory flow only, and all three card buttons are centered. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Tightened Reports card spacing (frontend only): removed the auto-expanding description spacer, reduced card minimum height, and reduced the description-to-button gap for all three actions. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Replaced the OCR template export with the uploaded approved format. The download now produces **OCR Activity Ledger** (15 columns, title/header colors, 28 protected entry rows, filters, conditional formatting, facility/unit dropdowns), **Guide**, and hidden **Formatting Codes** / **Unit Definitions** sheets. Active organization facilities populate the Facility dropdown. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.
- September 24, 2026 — Corrected the OCR-template reconstruction after directly reading the uploaded workbook XML/styles. The export now matches the real file: **Instructions → OCR Ledger → hidden Lookup values**, 15 exact headers, green required / blue optional / orange conditional header groups, the six source header comments, all 14 Guide entries, source column widths, row positions, filter/freeze settings, and the 500-row ledger range. This supersedes the previous inaccurate template note. **SOURCE-REVIEWED ONLY; NO FUNCTIONAL/API/BROWSER TESTING** per user instruction.